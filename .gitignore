# User-specific files for Visual Studio
.vs/
.vs2019/
.vs2022/
*.user
*.suo
*.userosscache
*.sln.docstates

# Build results
bin/
obj/

# .NET Core artifacts
artifacts/

# ASP.NET Core files
# Don't ignore wwwroot if static files (e.g., JS, CSS) are used in deployment
#wwwroot/

# Entity Framework
# Ignore local migrations, but **do not ignore migrations that should be tracked**
# Only ignore build artifacts related to migrations (EF's output files)
#Migrations/

# Swagger UI
# Swagger UI is usually static, but can be served from wwwroot, so don't ignore it if served from there.
# .gitignore for Swagger specific files is usually unnecessary, since it's generated from code.

# Logs (ignore local logs only, not logs required in the deployment environment)
*.log

# NPM/Yarn package manager logs (if you're using frontend or package-based tools)
npm-debug.log
yarn-error.log
package-lock.json
yarn.lock # Uncomment if you want to track package-lock or yarn.lock for reproducible installs

# Ignore environment-specific settings
# (Don't ignore these if you need them for deployment purposes, like appsettings.Production.json)
appsettings.Development.json
appsettings.Test.json

# Files generated by the build system (do not ignore files that are needed for deployment)
dist/
node_modules/

# Temporary files created by IDE
# Do not ignore if they are required by the build
*.db
*.bak

# Windows-specific files
Thumbs.db
ehthumbs.db
Desktop.ini
