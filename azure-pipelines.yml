trigger:
- develop

pool:
  vmImage: 'windows-latest'

variables:
  buildConfiguration: 'Release'  # Or 'Debug' or 'Staging' - YOU MUST CHOOSE
  appServiceName: 'AjTollSystem' #  <-- Correct App Service name
  resourceGroupName: 'AjTollSystem' # <--- Resource group name
  azureSubscriptionName: 'ServiceConnection' # <--- Service connection name

steps:
- task: NuGetToolInstaller@1
  displayName: 'NuGet tool installer'

- task: NuGetCommand@2
  displayName: 'NuGet restore'
  inputs:
    restoreSolution: '**/*.sln'

- task: DotNetCoreCLI@2
  displayName: 'Build'
  inputs:
    command: 'build'
    projects: '**/*.csproj'
    arguments: '--configuration $(buildConfiguration)'

- task: DotNetCoreCLI@2
  displayName: 'Test'
  inputs:
    command: 'test'
    projects: '**/*Tests.csproj'
    arguments: '--configuration $(buildConfiguration)'

- task: DotNetCoreCLI@2
  displayName: 'Publish'
  inputs:
    command: 'publish'
    projects: '**/*.csproj'
    publishWebProjects: true
    arguments: '--configuration $(buildConfiguration) --output $(Build.ArtifactStagingDirectory)'
    zipAfterPublish: true

- task: PublishBuildArtifacts@1
  displayName: 'Publish Artifacts'
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)'
    TargetPath: '$(Build.ArtifactStagingDirectory)' # Added TargetPath
    ArtifactName: 'drop'
    publishLocation: 'Container'

- task: DotNetCoreCLI@2
  displayName: 'Install dotnet-ef tool globally'
  inputs:
    command: 'custom'
    custom: 'tool'
    arguments: 'install --global dotnet-ef'

- task: DotNetCoreCLI@2
  displayName: 'Database migrations'
  inputs:
    command: 'custom'
    custom: 'run'
    arguments: 'ef database update --context Persistence.Contexts.Context --configuration $(buildConfiguration) --project Persistence --startup-project Api'
  condition: and(succeeded(), ne(variables['Build.Reason'], 'PullRequest'))

- task: AzureWebApp@1
  displayName: 'Deploy to Azure App Service'
  inputs:
    azureSubscription: '$(azureSubscriptionName)'
    appType: 'webApp'
    appName: '$(appServiceName)' # Correct App Service name
    resourceGroupName: '$(resourceGroupName)' 
    package: '$(Build.ArtifactStagingDirectory)/**/*.zip' # <--- Correct path
    deploymentMethod: 'zipDeploy'